import { adminStats } from '@/lib/mock-data'; import { Card } from '@/components/common/Card'; export default function AdminDashboardPage() { return <div><h1 className='mb-4 text-2xl font-semibold'>Admin Dashboard</h1><div className='grid gap-3 md:grid-cols-3'>{adminStats.map((s)=> <Card key={s.label}><p className='text-sm text-slate-500'>{s.label}</p><p className='text-xl font-semibold'>{s.value}</p></Card>)}</div></div>; }
