import { vendorStats, mockJobs } from '@/lib/mock-data'; import { Card } from '@/components/common/Card'; export default function VendorDashboard() { return <div className='space-y-4'><h1 className='text-2xl font-semibold'>Vendor Dashboard</h1><div className='grid gap-3 md:grid-cols-3'>{vendorStats.map((s)=> <Card key={s.label}><p className='text-sm text-slate-500'>{s.label}</p><p className='text-xl font-semibold'>{s.value}</p></Card>)}</div><Card><h2 className='mb-2 font-semibold'>Recent jobs</h2><ul className='space-y-2'>{mockJobs.map((j)=> <li key={j.id}>{j.id} • {j.status} • ETA {j.etaMinutes}m</li>)}</ul></Card></div>; }
